import { CreateLoanParams, Protocol, TransactionResult } from '@nftpawn-js/core';

export default class PawnProtocolNear extends Protocol {
  async createLoan(params: CreateLoanParams): Promise<TransactionResult> {
    
  }
}